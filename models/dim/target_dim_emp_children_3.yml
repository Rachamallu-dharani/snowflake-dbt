version: 2

models:
  - name: target_dim_emp_children_3
    columns:

      - name: child_name
        tests:
          - not_null

      - name: empid
        tests:
          - not_null
          - relationships:
              to: ref('src_staging_emp_data3')
              field: empid

      - name: child_gender
        tests:
          - accepted_values:
              values: ['M',
                       'F', 
                       'Unknown']
                       
      - name: child_age
        tests:
          - positive_value